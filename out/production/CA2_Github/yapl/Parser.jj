options {
    LOOKAHEAD = 2;
}
PARSER_BEGIN(Parser)

/** Match Grammar G from CA2.1 */
public class Parser {

  /** Main entry point. */
  public static void main(String args[]) throws ParseException {
    Parser parser = new Parser(System.in);
    parser.Input();
  }

}

PARSER_END(Parser)

/** Root production. */
 void Input() :
{}
{
    (A() | "a") "c"
}

void A() :
{}
{
    "a" A() | "b"
}
